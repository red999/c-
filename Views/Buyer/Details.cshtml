@model ShopV2.Models.Buyer

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Buyer</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.firmNameORsellerName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.firmNameORsellerName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.edrpouORidenticCode)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.edrpouORidenticCode)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Sign)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Sign)
        </dd>

        @* Begin addressbuyers *@
        <dd>
            <table class="table">
                <tr>
                    <th>addr info id</th>
                    <th>buyer id</th>
                    
                </tr>
                @foreach (var item in Model.AddressBuyers)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.AddressInfoID)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.BuyerID)
                        </td>
                        
                    </tr>
                }
            </table>
        </dd>
        @* END changeiteminfos *@
    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.ID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
