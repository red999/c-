@model IEnumerable<ShopV2.Models.Buyer>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.firmNameORsellerName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.edrpouORidenticCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Sign)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.firmNameORsellerName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.edrpouORidenticCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Sign)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
